# 用户手册（USER-GUIDE）—— Bot 使用指南

> 面向**群主 / 群管理员 / 群成员**：怎么用这个机器人、有哪些命令、会收到什么通知、它保存了你什么。
> 部署与配置见 `README.md`。

## 一、快速开始（群主三步）

1. **把机器人拉进群**，并给它**管理员权限**——删消息、封禁、`/teach` 的入群时长门槛都需要它。
2. 在群里发 **`/enable`**（需你的授权，见下一节）。在此之前，除恢复类命令外**一切命令都会被拒绝**——
   这是刻意的「门控只拒不放」。
3. 按需配置：`/addword` 加本群违禁词、`/teach` 教本群专属规则、`/group_tag` 声明群话题标签。

先发 **`/echo`**（或 `/ping`）确认连通——它会回一段固定文本。

## 二、权限：谁能用哪些命令

共 **4 条互相独立的授权通道**（不要在它们之间做类比）：

| 谁能用 | 由谁配置 | 配置为空时的后果 |
|---|---|---|
| **群内管理员** | 部署方设 `TGG_PERMISSION_ADMINS`（格式 `<chatId>:<userId>[:role]`） | **所有管理命令对任何人不可用**（不是「人人可用」） |
| **复核人** | 部署方设 `TGG_MODERATION_REVIEWERS` | `/review_*` 无响应（不是「权限不足」提示） |
| **联邦管理员** | 部署方设 `TGG_FEDERATION_ADMINS` | `/pending`·`/approve`·`/reject` 不可用 |
| **商家复核人** | 部署方设 `TGG_MERCHANT_REVIEWERS` | `/merchant_review`·`/merchant_deposit`·`/merchant_settle` 不可用 |

**命令的「需管理员权限」指上表第一行**。你自己的 userId 怎么取：给机器人发条私聊，然后
`curl -s "https://api.telegram.org/bot<Token>/getUpdates" | jq '.result[-1].message.from.id'`
（应用日志里的 id 是**哈希**过的，看不出来）。

## 三、命令一览（34 个）

> 计数由 `grep -rhoE '@BotCommand\(value = "[a-z_]+"' --include=*.java tgg-*/src/main | sort -u | wc -l` 实测得出。
> 手写计数会随功能增长静默失真（本行曾写 29，实际已达 34）——改命令时请重跑该命令核对。

### 基础

| 命令 | 用法 | 说明 |
|---|---|---|
| `/echo`（别名 `/ping`） | `/echo` | 连通性测试，回复固定文本 |
| `/help` | `/help` | 按分类列出**你**在本群能用的功能（纯文本，每条带说明） |
| `/status` | `/status` | 查看本群治理状态（是否启用、违禁词/教学规则/待复核各多少条） |

### 群设置（需群管理员）

| 命令 | 用法 | 说明 |
|---|---|---|
| `/enable` | `/enable` | 开启本群自动化能力（**必须**先执行，否则其余命令无效） |
| `/disable` | `/disable` | 关闭本群自动化能力 |
| `/group_tag` | `/group_tag add\|remove\|list [标签]` | 管理本群话题标签。**可豁免话题**：`politics` / `intl_politics` / `religionism`；**不可豁免**：恐怖活动 / 极端主义 / 煽动战争 |

### 本群违禁词（需群管理员）

| 命令 | 用法 | 说明 |
|---|---|---|
| `/addword` | `/addword <违禁词>` | 添加本群违禁词（命中即删并处置） |
| `/delword` | `/delword <违禁词>` | 删除本群违禁词 |
| `/words` | `/words` | 查看本群违禁词 |

### 本群教学规则

| 命令 | 用法 | 说明 |
|---|---|---|
| `/teach` | `/teach <规则id> <正则> <描述>` | 教一条本群审核规则（需 `TEACH_RULE`）。<br>例：`/teach SCAM_AIRDROP "免费空投\d+" 假空投骗局` |
| `/unteach` | `/unteach <规则id>` | 停用一条本群教学规则（需 `TEACH_RULE`） |
| `/rules` | `/rules` | 查看本群教学规则（需 `MANAGE_CONFIG`） |

⚠️ `/teach` 的正则要过**三道闸门**：能编译、长度 ≤512、拒绝灾难性回溯形态（嵌套量词）。
被拒是**保护**——一条 `(a+)+` 足以让机器人卡死在单条消息上。此外有**教学门槛**（无违规 / 无扣分 /
入群时长），未达标时不放行。

### 人工复核（需复核人白名单）

| 命令 | 用法 | 说明 |
|---|---|---|
| `/review_list` | `/review_list` | 列出待复核的审核命中 |
| `/review_approve` | `/review_approve <编号> [备注]` | **维持**结论（确认违规）。备注是裁决理由，**请勿粘贴消息正文** |
| `/review_reject` | `/review_reject <编号> [备注]` | **推翻**结论（判定误报）——这是防「误封真人」的安全网 |

**中高风险与硬红线都会入队**：硬红线已「立即删除 + 封禁」，入队是为了让你**事后推翻误封**（解封）。

### 审核案件申诉（限当事人）

| 命令 | 用法 | 说明 |
|---|---|---|
| `/case_appeal` | `/case_appeal <案件编号> <理由>` | 对某条审核案件提交申诉。<br>例：`/case_appeal 42 那条是正常讨论，不是广告` |

**案件编号从哪来**：消息被删除/冻结时，群内的处置告知会带上「案件 #42」——那就是编号；
在 Telegram 里翻到那条告知即可。也可用 `/case_appeal` 提到的那条通知。
**只有当事人能申诉**：编号在群内是公开的（告知里带着），但申诉权不公开——他人用同一编号提交会被拒。
**一人一案一次**：同一案件重复提交返回既有回执，不会产生第二条。

### 联邦申诉（模块八，需部署方启用）


| 命令 | 用法 | 说明 |
|---|---|---|
| `/appeal` | `/appeal` | 提交联邦申诉（解封申请） |
| `/pending` | `/pending` | 查看待审联邦申诉（联邦管理员） |
| `/approve` | `/approve` | 通过联邦申诉（联邦管理员） |
| `/reject` | `/reject` | 驳回联邦申诉（联邦管理员） |

### 群组收录（模块五，需部署方启用）

| 命令 | 用法 | 说明 |
|---|---|---|
| `/listing_add` | `/listing_add <邀请链接>` | 把本群加入收录库（需管理员权限，须在要收录的群内执行） |
| `/listing_list` | `/listing_list` | 查看本节点收录库（有效群组） |
| `/listing_appeal` | `/listing_appeal <收录编号> <理由>` | 对已下架的收录条目提交申诉 |

### 商家收录（模块六，需部署方启用）

| 命令 | 用法 | 说明 |
|---|---|---|
| `/merchant_apply` | `/merchant_apply <商家名称>` | 提交商家入驻申请 |
| `/merchant_status` | `/merchant_status` | 查看我的商家入驻状态 |
| `/merchant_review` | `/merchant_review <商家编号> approve\|reject\|need-more` | 商家资质复核（复核人） |
| `/merchant_deposit` | `/merchant_deposit <商家编号> <金额>` | 确认保证金并完成入驻（平台复核人） |
| `/merchant_exit` | `/merchant_exit <商家编号>` | 申请退出商家（触发保证金冻结） |
| `/merchant_settle` | `/merchant_settle <商家编号> <NONE\|UNRESOLVED\|WITH_COMPENSATION> [扣款额] [理由]` | 结算已冻结的保证金（平台复核人）。无争议全额退还；有未结争议保持冻结；有赔付则扣除后退还余额（扣款额与理由**都必填**） |

### 通知与隐私

| 命令 | 用法 | 说明 |
|---|---|---|
| `/quiet_hours` | `/quiet_hours 22:00-08:00`（支持跨午夜）<br>`/quiet_hours off`<br>`/quiet_hours` | 设置 / 取消 / 查看免打扰时段。⚠️ **封禁、解封等紧急通知不受免打扰影响，始终送达** |
| `/export_my_data` | `/export_my_data` | 导出机器人持有的**关于你本人**的数据（通知偏好 + 审计记录） |
| `/data_breach` | `/data_breach <影响范围> <影响人数>`<br>`/data_breach report <编号>`<br>`/data_breach` | 数据泄露登记（72 小时计时开始）/ 记录已通报 / 列出未通报事件。**通报是运营者的法定义务，本命令只做计时与留痕**（平台白名单） |

## 四、你会收到哪些通知

通知分**三级**，级别决定是否受免打扰影响：

| 级别 | 典型内容 | 免打扰时段内 |
|---|---|---|
| **紧急** | 被禁言 / 被解封、红线复核超时催办、泄露通报催办 | **照常送达** |
| 常规 | 审批待办提醒 | 延迟到时段结束后送达 |
| 汇总 | 低频聚合 | 延迟送达 |

**没有配置 bot token 时**，通知退化为**日志实现**——你不会在私聊里收到任何东西（启动日志有 WARN 明示）。

## 五、隐私：机器人保存了你什么

- **消息正文零存储**：数据库表里没有承载消息正文的字段；正文在当次处理结束时（含异常路径）从内存清除。
- **日志脱敏**：你的 userId 与 chatId 在日志中一律是**哈希值**（HMAC-SHA256）。
- **审核用到的正文**不会写入日志，连「命中了哪个片段」都不记（只记规则 id 与风险等级）。
- **例外一**：启用 L3 云端审核时，**正文会发送至第三方 AI 服务**（默认关闭，且属数据出境，需运营者完成告知）。
- **例外二**：复核队列表存**明文** userId/chatId（用途是让复核人定位并处置当事人；哈希后无法定位）。
  该表**不含消息正文**。
- 想导出关于你自己的数据：`/export_my_data`。⚠️ **目前没有自助删除**——审计表刻意不可删（合规留痕），
  其余数据由运营者按保留策略清理。

细节与法域相关说明见 `PRIVACY.md`。

## 六、常见问题

**Q：发了命令没有任何反应？**
按顺序查：① 群里是否执行过 `/enable`；② 你的 userId 是否在白名单内（**不在**时通常**无响应**，
而不是回一句「权限不足」）；③ 对应模块是否被部署方启用（默认全部关闭）。

**Q：我自己就是群管理员，为什么还是用不了管理命令？**
Telegram 的管理员身份**不被本项目直接采信**——授权来自部署方的 `TGG_PERMISSION_ADMINS` 配置。
请让部署方把你的 `<chatId>:<userId>` 加进去。配置为空时**所有人**都不可用。

**Q：消息被删了但我没违规？**
这是误报。请让复核人在 `/review_list` 里找到该条并 `/review_reject` 推翻——**硬红线也能被推翻**
（会同时解封）。若你已因联邦标记被跨群封禁，走 `/appeal`。

**Q：`/teach` 的正则被拒绝了？**
只可能是三个原因之一：编译不通过、超过 512 字符、含嵌套量词（如 `(a+)+`、`((a+))+`）。
最后一条是**故意**的：那类正则会灾难性回溯，让机器人卡死在单条消息上。

**Q：机器人会读我的私聊吗？**
只处理发给它命令的那些更新；私聊不是它的运营场景（`/start` 等未实现）。群里的普通消息则会经过
审核流水线，但**正文零存储**（见上一节）。
