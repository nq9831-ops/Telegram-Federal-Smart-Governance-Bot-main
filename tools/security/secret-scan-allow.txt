# 敏感信息扫描的**豁免清单**（被 tools/security/scan_secrets.sh 读取）
#
# 格式：<pattern名> <路径子串> [# 理由]
#   pattern 名：telegram-bot-token / private-key-block / hardcoded-credential / aws-access-key
#   路径子串：匹配 file:line 里的相对路径（相对仓库根）
#
# ⚠️ 纪律：**只豁免已确认的误报或刻意的测试值**，并在行尾写清理由。
#    绝不要为了让 CI 变绿而改宽扫描正则——那会让真正的新密钥一起漏报。
#
# 当前清单：空。
#   · 扫描含 `src/test/resources/`（`test-secret-value` / `test-bot-token` / `tggdev` 这些
#     刻意的测试值）与 `tools/deploy/all-on.env.example`（`<...>` 占位符）——实测**均未误报**，
#     因为正则要求 20 位以上的真实字符，占位符与短测试值都达不到阈值。
#   · 若将来确有误报，按上面的格式追加一行，不要动 scan_secrets.sh 的正则。
