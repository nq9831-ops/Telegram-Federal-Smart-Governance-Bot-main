# Secret scanning allowlist
#
# Keep this file empty unless a confirmed false positive requires an explicit,
# narrowly scoped exception with a documented reason. Never use it to suppress
# a real credential or to weaken the scanner patterns.
