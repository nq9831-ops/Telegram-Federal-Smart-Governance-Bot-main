# 语气规范（VOICE）

> 本文件是机器人**用户可见文案**的唯一规范来源。守门测试
> （`tgg-app/src/test/java/com/tg/heyisheng/bot/VoiceConformanceTest.java`）的判据取自这里的「可机器判定」条目；
> 主观条目靠评审遵守。
>
> **本文件进版本库**（仓库根的规范族，与 `README.md` / `PRIVACY.md` / `SECURITY.md` 并列）——
> 源码 javadoc 直接引用它（如 `core/interaction/InteractionMessages`），若放在不进 git 的 `docs/` 下，
> 新克隆的仓库里引用就会悬空。

---

## 一、人格：亲切得体的助手，**不做真人扮演**

机器人是**部署方的助手**，语气亲切、得体、稳定。

**不做什么**（这条是硬边界，用户明确要求）：

- **不扮演真人**：不设定性别、姓名、年龄、外貌或与用户的情感关系；不使用"人家""小女子""亲""抱抱"这类拟人亲密措辞；
- **不卖萌、不撒娇、不情绪化**：不用网络热词与颜文字堆砌；
- **不代替运营者表态**：涉及规则判定、处罚、资金时，只陈述**已发生的事实**与**下一步**，不替运营者承诺或解释动机。

**做什么**：说清楚"发生了什么、你该怎么做"。用户看不到后台，文案是唯一的解释面。

## 二、称呼与自称

- 对用户一律用「**你**」（不用"您"，也不用"该用户"）；
- 机器人自称「**我**」；不称"本机器人""系统""AI"；
- 只有确指平台层角色时才用「平台复核人」「群管理员」这类第三人称。

## 三、语气基线

1. **结论先行**：第一句就说结果（"已提交""已被删除""不可用"），第二句再说下一步或依据。
2. **拒绝要给出去路**：任何"不可/不能"后面必须跟"你可以怎么做"或"有疑问找谁"。
3. **不责备用户**：不说"你怎么又…""请勿重复提交"这类教训口吻；改陈述客观事实。
4. **不卑不亢**：不用连串道歉；一次失误说一次"抱歉"，随后给结论与补救。
5. **不过度承诺**：不写"立即""马上"之类无法保证的时间词（除非系统确实即刻动作）。

## 四、标点与格式

- 中文文案用**全角**标点（，。：；「」），句末带句号（列表项、按钮文案除外）。
- 列表项用「· 」或「1. 」，同一段内不混用。
- **命令示例必须可照抄**：给完整可执行的例子（`/quiet_hours 22:00-08:00`），**不得**给参数模板（`/cmd <参数>`、`approve|reject`）——模板符号会被运营者整串照抄发出去，命令一直回用法、看起来像坏了。
- 数字、ID、命令名**原样输出**，不加千分位、不缩写。

## 五、emoji 政策（白名单）

只允许这四个：

| emoji | 唯一用途 |
|---|---|
| `⚠️` | 用户需**立刻注意**的处置结果（消息被删、已被封禁、内容被分级处理） |
| `✅` | **按钮**文案（确认执行） |
| `❌` | **按钮**文案（取消） |
| `←` | **按钮**文案（返回） |

正文里不堆 emoji；不用其它 emoji（尤其表情类）。

## 六、三类句式

| 类型 | 形状 | 例 |
|---|---|---|
| **回执** | 「已<动作>（<编号>），<后续会怎样>」 | 申诉已提交（编号 #12），将由联邦管理员审核。 |
| **拒绝** | 「<谁>才能<做某事>。+ 出路」 | 只有商家本人可以申请退出。 |
| **用法** | 「用法：\n/命令 可照抄示例 —— 说明」 | 用法：\n/quiet_hours 22:00-08:00 —— 设置免打扰时段 |

## 七、禁止项

### 可机器判定（守门测试直接查）

| # | 禁止 | 判据 |
|---|---|---|
| 1 | 模板符号 | 用户可见字面量同时含「斜杠命令」与 `<` `>` `\|` |
| 2 | 未填占位符 | 文案里出现 `{0}`-`{9}`、`XXX`、`TODO`、`FIXME` |
| 3 | 就地散落 | 用户可见字面量必须来自 `*Messages` 常量，不得内联在调用点 |

判据 3 的**覆盖范围**由 `VoiceConformanceTest.MIGRATED_PACKAGES` 声明（随搬迁推进逐包扩大）：
匹配「用户可见调用点」行（`.text(` / `answer(` / `reply(` / `sendMessage(` / `SendMessage(` / `return`），
并排除注释行与含 `log.` 的行（日志不是用户文案）。

### 靠评审遵守（难机器判定）

4. 真人扮演式措辞（"人家""亲""小女子"…）；
5. 机器腔（"系统检测到""您的请求"）；
6. 责备用户；
7. 使用白名单外的 emoji。

## 八、文案的落点规则（`*Messages` 的组织方式）

1. **完整句子** → `*Messages` 里的常量；
2. **含插值的句子** → `*Messages` 里的静态方法（如 `quietHoursSet(range)`、`identityGroup(chatId, userId)`）——不硬拆成碎片常量；
3. **纯格式胶水**（`/`、` — `、`「」`）→ 留在各自的视图/格式化处；
4. **组合报表**（多段插值拼装的整段输出）→ 留在原处，不为搬迁造碎片常量；
5. 已被外部（测试等）引用的常量，搬迁时保留**同名别名** `= Messages.X`，避免为搬迁去改测试。

## 九、本规范与守门测试的分工

- 第 7 节的三条**可机器判定**项由 `VoiceConformanceTest` 强制；
- 其余条目在**评审**时核对。新增文案前先读本文件；新增 `*Messages` 类时逐条对照。

> **为什么不让机器判定语气**：语气是主观的，硬编码词表必然随新文案过窄或过宽（本项目已有"维护一份清单必然悄悄过期"的教训）。机器只守**客观可判**的三条，其余交评审。

---

## 十、语气变换规则（R1–R7，铺语气时的执行判据）

把「亲切得体」从原则落到 900+ 行文案上，必须给出**可机械执行**的规则，否则必然风格漂移。
逐条应用，不凭感觉：

**口语档位（2026-09-21 用户确认）**：全量采用**日常对话**档——短句优先；用口语助词（吧／呢／了）与
「得…才…」「…就行」「…看一眼」这类轻口语句式；疑问式引导代替宣告；去掉括号补充与书面连接词（"因此""该""请务必"）。

**硬边界不变**：不卖萌、不撒娇、不堆网络热词、不真人扮演（第一节）；emoji 白名单不动（第五节）；
**事实面一律保留**（见本节末）。

| # | 规则 | 反面（不要） |
|---|---|---|
| **R1** | **结论先行**：先给结果（含编号/状态），再给依据或下一步 | 先解释背景、结果埋在末句 |
| **R2** | **拒绝给去处**：任何"不可/不能/没有/失效"后必须跟一句出路（"你可以…"/"有疑问找谁"） | 只宣布拒绝，用户看完不知道能做什么 |
| **R3** | **去机器腔**：`该用户/本机器人/系统/已被处理` → `你/我` + 主动语态 | 被动句、第三人称自称 |
| **R4** | **收尾统一**：需要用户后续动作的，以祈使句收尾（「请…」）；不需要动作的**不加客套**（不写"谢谢"） | 一律加"谢谢""感谢您的配合" |
| **R5** | **标点与 emoji**：按第四、五节（`⚠️` 只用于处置结果；`✅`/`❌`/`←` 只用于按钮；正文不堆表情） | 用表情表达态度 |
| **R6** | **技术面克制**：配置键描述与 REST 错误**以精确为先**——套用 R1/R3，但**不口语化、不加称呼**。读者是运维，要一眼判断后果 | 把运维排查文案写成聊天口吻 |
| **R7** | **Web/前端同一人格、书面语境**：状态 + 后果为主；**不写命令引导式的祈使句**（按钮本身就是动作） | 在 Web 界面上写"请发送 /xxx" |

**不变的事实面**（改语气时逐条核对，不得丢失或改错）：编号（`#N`）、时限（`SLA N 小时`、`72 小时`、`N 天`）、
状态词（`PENDING`/`ACTIVE` 等及其中文对应）、**可照抄的命令示例**（`/quiet_hours 22:00-08:00` 这类必须仍可原样发出）。

## 十一、示例对照（铺语气的执行判据）

> 左列是**当前生产中的真实字符串**，右列是套用 R1–R7 后的目标。全部保留事实面。
> 本表经确认后即为全量执行的判据来源；后续每波只做"同规则套用"，不再逐条讨论。

### 自助 / 交互（Wave 1）

| 现有 | 目标 |
|---|---|
| `你在此群没有可用的功能。` | `这个群里暂时没有你能用的功能。私聊我，我告诉你都有哪些。` |
| `操作过于频繁，请稍后再试。` | `点得有点快，稍等一下再试。` |
| `该确认已失效（超时或已被使用），请重新发起。` | `这张确认卡失效了——可能超时了，也可能已经用过。重新发一次命令就有新的。` |
| `请在要生效的群内执行本命令。` | `这条命令得在群里发才管用。` |
| `你的 Telegram 用户 ID：\n<数字>\n\n配置复核人、群管理员等白名单时需要它。` | `你的 Telegram 用户 ID 是：\n<数字>\n\n填白名单（复核人、群管理员）的时候填这个数就行。` |
| `可用功能（只列出你在此群能用的）：\n选一个分类查看，点按钮直接执行。` | `这些是你在本群能用的功能：\n先点个分类，再点按钮就能用。` |
| `你当前没有设置免打扰时段（所有通知都会送达）。` | `你还没设免打扰，消息都会照常送到。` |
| `你本来就没有设置免打扰时段。` | `你还没设过免打扰，不用取消。` |
| `已取消免打扰时段。` | `免打扰取消了。` |
| `pong`（`/echo` 固定回复） | `在的，连通没问题。` |

### 通知 / 平台面（Wave 2）

| 现有 | 目标 |
|---|---|
| `\n（另有 N 条同类通知已合并）` | `\n（另外 N 条同类的并进来了）` |
| `⚠️ 审批升级：待办 #N（等级 X，已积压 H 小时） 仍未处理，请登录治理后台处理。` | `⚠️ 审批升级：待办 #N（等级 X，已经积压 H 小时）还没处理，去治理后台看一眼吧。` |
| `⚠️ 硬红线复核超时（SLA H 小时）：复核 #N 仍未裁决（规则 R）。请在 /review_list 中裁决：/review_approve 维持 或 /review_reject 推翻并解封。` | `⚠️ 硬红线复核超时（SLA H 小时）：复核 #N 还没裁决（规则 R）。用 /review_list 看一眼，然后 /review_approve 维持，或者 /review_reject 推翻并解封。` |
| `⚠️ 数据泄露事件 #N 尚未通报：<范围>（预计影响 N 人，通报截止 <时间>）。请尽快履行通报义务并执行 /data_breach report N 记录。` | `⚠️ 数据泄露事件 #N 还没通报：<范围>（预计影响 N 人，截止 <时间>）。通报做完之后，用 /data_breach report N 记一笔。` |
| `这是一个提醒。` 类无动作通知（若有） | 直接给状态，不写"这是一个提醒" |

### 惩戒类（Wave 5，**另审**——此处仅给方向）

| 现有 | 目标（方向） |
|---|---|
| `⚠️ 该消息命中本群内容规则，已被删除。如对判定有疑问，请联系群管理员。` | `⚠️ 这条消息命中了本群内容规则，已经删掉了。有疑问直接找群管理员就行。` |
| `⚠️ 该消息命中本群硬性红线，已被删除，发布者已被封禁。如对判定有疑问，请联系群管理员。` | `⚠️ 这条消息命中了本群硬性红线，已删除，发布者也封禁了。有疑问请联系群管理员。` |
| `⚠️ 本群已开启敏感话题分级：该内容已被处理。再次发布将禁言 24 小时。` | `⚠️ 本群开了敏感话题分级，这条内容已经处理掉了。再发一次会被禁言 24 小时。` |

> **惩戒类的克制原则**：事实要**重**、语气要**稳**——不加重情绪、不训诫、不解释动机。
> 被告知者可能正在不满，此时"亲切"不等于"轻松"，只等于"清楚且不刺人"。
> 故此类**只做口语化，不做情绪化**：删掉书面腔（"已被…"），但不加调侃、不加安慰语。

### 配置键描述（Wave 4）

| 现有 | 目标 |
|---|---|
| `复核人 userId 白名单。为空则 /review_* 对任何人不可用（启动 WARN）。热生效。` | `谁能当复核人（userId 白名单）。留空的话，/review_* 谁都点不了（启动时会告警）。热生效。` |
| `数据库连接串。应用启动的前提，只读。` | `数据库连接串——应用启动的前提条件。只读。` |

**R6 判据**：描述里必须写清「不设会怎样」；可轻度口语，但**不加称呼、不写"我帮你…"**。

> ⚠️ **机器校验 token 不能丢**：可写的运行期键，其描述必须含字面量「**热生效**」或「**需重启**」
> ——`ConfigCatalogTest.everyWritableRuntimeKeyDeclaresHotOrRestart` 直接断言这条。
> 改描述时这两个词**原样保留**（它们已被测试钉住）。

### Web 前端（Wave 3）

| 现有 | 目标 |
|---|---|
| `未设置`（配置项生效值占位） | 保持（状态占位符，短而准） |
| `<未设置>`（`ConfigController` 返回值） | `（未设置）`——角括号在纯文本渲染里不是占位符语法，只会让人以为是模板 |
| 前端错误提示（`frontend/src/api/client.ts`） | 状态 + 后果 + 用户能做什么；**不写**"请发送 /xxx"（R7） |

> Web 侧的落地尺寸必须确认：前端文案改动**不能**只靠 grep 判断是否需要同步单测
> （本仓 `toBe("中文")`/`toContain("中文")` 命中 0 属**推断**，Wave 3 须实跑 `pnpm test` 验证）。
