# 隐私说明（PRIVACY）

> 本文件说明本机器人处理消息数据的边界。**开发者不参与运营**——实际部署方（运营者）须自行确认
> 与其所选第三方服务（如 DeepSeek）之间适用的条款与留存策略，并对合规负责。

---

## 一、基本原则

1. **不存聊天记录**：消息原文不落库、不写日志。审核所需内容在内存中即时消费后由
   `MessageScrubber` 清除（`text`/`caption`/投票文本/地点/引用/回复链等承载文本的字段一并清除）。
2. **日志只记结论，不记内容**：审核日志只含规则 id、风险等级与**哈希化**的群/用户标识
   （`IdHasher`，HMAC-SHA256 加盐），不含消息正文。
3. **配置类数据可持久化**：违禁词表、群配置、待复核队列、**信用分账本、联邦账本与申诉**等属**配置与结论**，
   与"聊天内容"不同。其中复核队列、信用分账本与联邦账本含明文 `chat_id`/`user_id`（用途是人工复核、
   施加处罚或跨群封禁时定位主体，哈希后无法定位），部署方须确保库访问受控。
   联邦申诉的 `appeal_text` 是用户**主动提交**的申诉正文，不是被监听的对话内容。

## 二、第三方 AI（本文件中最重要的声明）

**L3 审核层会把消息正文发送给 DeepSeek 云端**——这是唯一会把消息内容送出部署环境的环节。

| 事项 | 说明 |
|---|---|
| **默认状态** | **关闭**。`tgg.ai.deepseek.enabled` 默认 `false`；不显式开启则**不发送任何内容** |
| **发送内容** | **仅消息正文文本**。**不含** `userId`、`chatId`、群名、用户名等任何身份标识（`ModerationLayer.inspect(String)` 的契约天然只有文本，代码注释明确禁止给该层加身份参数） |
| **发送时机** | 仅当消息**未**被更早的层（L1 正则、L2 本地）判定命中时——命中即短路，不会重复外发 |
| **接收方** | `tgg.ai.deepseek.base-url` 指定的端点（默认 `https://api.deepseek.com`） |
| **第三方留存** | ⚠️ **由 DeepSeek 的服务条款决定，本项目无法保证**。部署方在启用前**必须**自行确认其数据保留/训练使用政策，并向群成员作出必要告知 |
| **失败行为** | 第三方不可用时该层**放行**（fail-open，L1 仍在防线内）。注意：此期间**该层等于未审核**，日志会留痕 |

**给部署方的要求**：启用 L3 前，请把本节内容并入你对群成员的告知；若你的司法辖区要求
额外同意流程，请自行补充。

## 三、本地模型（L2 / L4）

本地模型层（L2 ML、L4 零样本）**不构成第三方数据传输**——推理发生在部署方自备的服务上。
这正是设计选择"DeepSeek 为主 + 本地安全模型补充"的用意：本地层可化解把内容送往第三方的隐私张力。

- 默认**不装配**（`tgg.ai.local.l2-enabled` / `l4-enabled` 默认 false）。
- 与 L3 同口径：只发正文，不发身份信息。

## 四、身份标识的处理

| 位置 | 处理方式 |
|---|---|
| 日志 | **哈希化**（`IdHasher`，加盐 HMAC；盐由 `TGG_HASH_SALT` 注入，缺失时会告警） |
| 复核队列表 | 明文（用途是人工定位；属配置/结论数据） |
| 短信验证按钮的 `callbackData` | **哈希化**（按钮对全群可见，明文会暴露 userId） |
| 送往第三方 AI | **不发送** |
| 信用分账本（`credit_scores`） | **明文**（用途是施加处罚时定位主体；属结论数据，不含正文） |
| 联邦账本（`federation_penalties`） | **明文**（用途是跨群封禁时定位主体；属结论数据，不含正文） |
| 联邦申诉（`federation_appeals`） | **明文**（用户主动提交的申诉正文，非被监听内容） |

## 五、部署方的控制手段

- 不启用 L3（`tgg.ai.deepseek.enabled=false`）→ 消息内容不外发。
- 不启用观察期（`tgg.admission.observation-seconds=0`）、不启用入群验证（`tgg.admission.enabled=false`）。
- 通过环境变量注入所有密钥，**不要**写进配置文件或提交到仓库。

## 六、数据清理

开发期测试数据（`group_configs`、`moderation_review_queue`、`banned_words` 中的测试记录）
应在正式运营前清理——见 `docs/DEPLOYMENT-VERIFICATION.md` E 段。
