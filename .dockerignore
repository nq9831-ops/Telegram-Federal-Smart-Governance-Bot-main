# Docker 构建上下文瘦身：context 会整体传给 daemon，这里把不需要进镜像的都排掉。
# （独立于 .gitignore；本地开发资产不入库，也不该进镜像。）

# 构建产物
**/target/

# 版本控制
.git/

# 本地开发资产
docs/
.rivet/
references/
scripts/

# 构建工具：容器内构建不需要（只用 pom + src）
tools/

# 前端：独立静态站点，不进后端镜像（构建上下文也不必带上它的依赖与产物）
frontend/

# 编辑器 / 系统杂项
**/.DS_Store
**/*.log
.idea/
*.iml
.vscode/
*~

# 本地数据库与临时探针文件
**/*.db
**/*.db-shm
**/*.db-wal
*.raw
