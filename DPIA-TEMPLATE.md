# DPIA 模板（数据保护影响评估）

> ⚠️ **这是模板，不是一份已完成的评估。** 本项目的开发者**不参与运营**，无法代替运营者作出法律判断。
> 运营者须按自己的**实际部署**（启用了哪些模块、接了哪些第三方、面向哪个法域）填写、签署并留存本文件。
> 本模板只提供**结构**与「本项目实际会处理哪些数据」的事实依据。
>
> 配套文件：`PRIVACY.md`（隐私说明模板）、`COMPLIANCE.md`（运营者合规清单）、`SECURITY.md`（安全控制）。

GDPR 第 35 条要求在「处理可能对自然人权利与自由带来高风险」时开展 DPIA。判断是否需要，
先看你的部署是否命中下列任一情形：

- [ ] 对个人进行**系统性监控**（本项目的审核流水线属于此类，尤其在中高风险命中会入队人工复核）
- [ ] 大规模处理**敏感类别**数据（本项目会处理「敏感话题」命中记录）
- [ ] 大规模处理未成年人数据（**若你的群面向未成年人，请注意本项目未实现年龄核验**）
- [ ] 将**消息正文**发送至第三国（仅当你启用 L3 云端审核时成立）

## 一、处理活动描述

**填写项**（运营者填）：

| 项 | 你的部署 |
|---|---|
| 控制者名称 / 联系方式 | |
| 部署的群组数量级与人数规模 | |
| 启用的模块（默认全部关闭） | |
| 是否启用 L3 云端审核（正文出境） | |
| 数据存放位置（库服务器所在法域） | |

**本项目实际会处理的数据**（代码与 Flyway 迁移实证，按数据表归属）：

| 数据 | 明文标识 | 正文 | 用途 |
|---|---|---|---|
| 群配置（`group_configs`） | 明文 `chat_id` | — | 群开关状态 |
| 违禁词库（`banned_words`） | 明文 `chat_id` | — | 按群词库 |
| **复核队列**（`moderation_review_queue`） | ⚠️ **明文 `chat_id` / `user_id`** | ❌ 无 | 人工复核定位当事人 |
| 教学规则（`moderation_rules`） | 明文 `chat_id` | — | 按群正则规则 |
| 群话题标签（`group_topic_tags`） | 明文 `chat_id` | — | 敏感话题豁免 |
| 敏感话题计数（`sensitive_topic_strikes`） | 明文 id | — | 递进处置 |
| 通知偏好 / 延迟队列（`notification_preferences` / `notification_deferred`） | 明文 `user_id` | — | 免打扰时段 |
| **审计日志**（`audit_log`） | 明文 actor/target | ❌ 无 | 处置留痕，**不可删** |
| 泄露事件（`data_breach_incident`） | 视填写内容 | — | 72h 计时与留痕 |
| 入群时间（`member_join_observations`） | 明文 id | — | §10.3 教学门槛 |
| 信用分（`credit_scores`） | 明文 id | — | 三套信用分 |
| 联邦记录（`federation_penalties` / `appeals`） | 明文 id | — | 跨群封禁与申诉 |
| 收录与商家（`listing_*` / `merchants` / `merchant_*`） | 明文 id、邀请链接、资质材料 | — | 收录与保证金 |
| **应用日志** | ✅ **哈希化**（HMAC-SHA256） | ❌ 无 | 运行诊断 |

**消息正文**：**零存储**——表结构与实体没有承载正文的字段；正文在当次处理的 `finally` 中从内存清除
（含异常路径）。**例外**：启用 L3 云端审核时，正文会发送至第三方 AI 服务（默认关闭）。

## 二、必要性与相称性

对照 GDPR 的数据保护原则，本项目对应的**已实现**能力：

| 原则 | 本项目 |
|---|---|
| 合法性、公平、透明 | 需运营者确定合法性基础（通常为「正当利益」），并以 `PRIVACY.md` 告知 |
| 目的限制 | 数据仅用于群治理；无二次营销用途 |
| **数据最小化** | 正文零存储；`UpdateContext` 只带路由元数据；日志只记哈希 id |
| 准确性 | 信用分与计数有明确规则来源（规则引擎） |
| **存储限制** | 保留策略可清理复核队列 / 违规计数 / 入群观察；**审计表永不清理**（刻意） |
| 完整性与保密性 | 见 `SECURITY.md`（常量时间密钥比对、RBAC、限流、注入防护） |
| 可问责 | 审计表 + 不可删；本 DPIA 本身 |

⚠️ **必须由运营者补齐的三项**：① 合法性基础的法律判断；② `PRIVACY.md` 按实际部署改写（尤其 L3 与出境）；
③ 保留期限的确定与告知。

## 三、风险清单与缓解

| 风险 | 本项目已有控制 | 运营者仍需做 |
|---|---|---|
| 消息正文被留存/泄露 | 零存储 + `finally` 清除 + 日志不记片段 | 确认 L3 的开关状态；若启用，完成告知/同意 |
| ⚠️ **复核队列含明文 id** | 队列**不含正文**；库访问须受控 | 限制库访问权限、加密存储介质、控制备份 |
| 标识被反推（userId 空间小） | `IdHasher` + `TGG_HASH_SALT` | **必须**配置强随机盐（否则退回开发兜底盐） |
| 正文出境（L3） | 默认关闭；启用即需要密钥 | 评估法域要求（标准合同 / 认证），完成告知 |
| 未成年人数据 | ⚠️ **未实现年龄核验** | 若面向未成年人，须自行设计与承担该义务 |
| 审计数据长期留存 | 刻意不可删（合规留痕） | 在隐私政策中说明该例外及其期限依据 |
| 数据主体无法自助删除 | 保留策略 + 人工流程 | 建立人工响应流程，并在政策中写明 |
| 服务被洪泛/拖垮 | 三级限流 + 正则 ReDoS 闸门 | 反向代理层限速与告警 |
| 依赖漏洞 | SBOM + OSV 扫描（CI 自动） | 基础镜像另行扫描；及时升级 |

## 四、与数据主体协商

- [ ] 是否已通过 `PRIVACY.md` 向群成员**充分告知**处理的类别、目的、保留期与权利行使方式？
- [ ] 若启用正文出境，是否已完成**单独的告知/同意**流程（而非笼统引用隐私政策）？
- [ ] 是否提供了可用的联系渠道以行使查阅/删除/反对权？

## 五、结论与签署

| 项 | 内容 |
|---|---|
| 评估结论 | （风险是否可接受 / 需先采取哪些措施） |
| 是否需要事先咨询监管机构 | 若残余风险仍为「高」，GDPR 第 36 条要求事先咨询 |
| 评估人 / 日期 | |
| 复核周期 | 建议至少每年一次，或在**启用新模块 / 新第三方**时重做 |

> **再次提醒**：本文件是模板。开发者只提供软件，不参与运营，也不代替你作出法律判断。
> 若你的业务涉及虚拟资产转移，请注意本项目**不提供**资金托管、制裁筛查与 KYC（见 `COMPLIANCE.md` 开篇）。
