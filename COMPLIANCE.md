# 运营者合规清单（COMPLIANCE）

> **本项目只提供软件。** 开发者不参与运营、不提供担保、不代替运营者作出法律判断。
> 本清单的作用是把「**软件能做什么、不能做什么**」摊开，供运营者据此完成自己的合规评估。
> 它**不是**法律意见，也不能替代律师。
>
> 下文每节都区分「**已实现**」与「**未实现**」。对未实现的部分，请勿假定本项目已提供该能力。

---

## 一、先读这一节：本项目**没有**做什么

以下几项与金融/制裁合规直接相关，均属原文的后续模块，**当前代码库不含**（详见 `docs/KNOWN-ISSUES.md`）：

| 能力 | 状态 | 含义 |
|---|---|---|
| TON 链上托管 / USDT 支付 | **未实现**（模块十二） | 无资金托管、无链上转账、无智能合约。`DepositGateway` 当前是 noop 实现，每次调用只打 WARN 日志 |
| OFAC / SDN 制裁筛查、KYT | **未实现** | 无任何名单筛查能力 |
| KYC / FATF 旅行规则 | **未实现** | 不采集、不传输发起人/受益人身份信息 |
| Web 后台界面 | **已交付**（后端 REST API + `frontend/` Vue 3 静态控制台） | 静态站需自行部署并配反向代理；渲染只在本机探针验证过，未在真实浏览器人工确认观感 |
| 设备指纹 / 行为序列 / 图计算等风控 | **未实现** | — |

**若你的业务涉及虚拟资产转移，本项目当前不满足相关合规要求。**

---

## 二、数据保护（GDPR 等）

| 要求 | 本项目对应能力 | 状态 |
|---|---|---|
| 数据最小化 | 消息原文**零存储**；日志只记规则 id / 风险等级 / **哈希化**的群与用户标识（`IdHasher`） | **已实现** |
| 默认隐私 | 会把正文外发的 L3 云端审核**默认关闭**；准入与观察期默认关闭 | **已实现** |
| 存储限制 | 保留策略可清理复核队列 / 违规计数 / 入群观察；**审计表永不清理**（刻意） | **已实现**，需部署侧显式放行 `tgg.retention.enabled=true`（默认只报告不删） |
| 数据主体查阅权 | `/export_my_data`——只导出**发起者本人**的通知偏好与审计记录 | **已实现** |
| 数据主体删除权 | ⚠️ **无自助删除**。审计表刻意不可删（合规留痕），其余数据由保留策略按期限清理 | **部分实现** |
| 72 小时泄露通报 | `/data_breach` 提供登记 + 计时 + 催办 + 留痕 | **已实现**，但**不代替运营者履行通报义务**——软件不会向监管机构或当事人发送任何东西，是否构成「需通报的泄露」是法律判断 |
| 数据出境 | L3 会把**正文**送往 `api.deepseek.com`（境外） | **仅提供开关**；能否启用取决于你的法域与告知/同意流程（见 `PRIVACY.md` 第二节） |

---

## 三、AI 治理（EU AI Act 等）

原文要求「高风险 AI 系统需有效人工监督，操作员有权推翻」。本项目的对应实现：

- **AI 只给建议，不自动裁决**：中高风险命中一律进**人工复核队列**。
- **操作员有推翻权**：`/review_reject`（Telegram 端）或 `POST /admin/approvals/{id}/decide`（Web 端）。
  推翻一条**硬红线**会**解封**当事人——这是防「误封真人」的安全网。
- **红线由规则而非模型判定**：命中即冻结的硬红线来自代码内置规则（不经模型），其误判同样可被人工推翻。
- **可审计**：每次处置写入审计表，且审计表**不可删**。

⚠️ **这不等于符合 AI Act**：高风险系统判定、透明度义务、技术文档等要求须运营者自行评估。

---

## 四、未成年人（COPPA 等）

原文要求「13 岁以下需可验证的家长同意」。本项目**未实现年龄确认流程**。
若你的群面向未成年人，须自行设计并承担该义务。

---

## 五、Telegram 平台政策

| 要求 | 本项目对应 |
|---|---|
| 必须提供隐私政策 | 见 `PRIVACY.md`（**模板**，运营者须按自身部署改写后公开） |
| 只采用 TON | 本项目未集成其他链；TON 相关功能本身也**未实现** |

---

## 六、安全防护（对应原文 §15.2）

| 项 | 状态 |
|---|---|
| 密钥经环境变量注入、不硬编码 | **已实现**（关键密钥缺失即 fail-fast 启动失败） |
| 日志脱敏（标识哈希化） | **已实现** |
| 依赖版本统一管理 | **已实现**：版本由父 POM 统一管理（含下列几项漏洞修复的版本覆盖） |
| SBOM（软件物料清单） | **已实现**：`mvn package` 生成聚合 `target/bom.json`（CycloneDX 1.5，`cyclonedx-maven-plugin`），覆盖全部模块与编译/运行期传递依赖 |
| 依赖漏洞扫描 | **已实现且可复现**：`tools/security/scan_dependencies.py` 读 SBOM 查 OSV（osv.dev）。本仓库最近一次扫描（2026-09-19）**0 命中**——此前发现的 3×CRITICAL/5×MEDIUM 已通过版本覆盖处置，记录见 `docs/KNOWN-ISSUES.md`。⚠️ **扫描只覆盖公开收录的漏洞，不等于安全保证**；上线环境应把它接进 CI 持续跑 |
| 容器化部署 | **已实现**：`Dockerfile`（多阶段构建 + 非 root 运行）+ `docker-compose.yml`（应用 + MySQL）。**并**提供 K8s 清单（`k8s/`，未经集群验证） |
| Token 定期轮换 | **未实现**（属运营流程，非代码能力） |
| Vault 集成 | **未实现**（用环境变量替代） |
| Actuator 锁定 / 安全响应头 | **已实现**（原文 §15.2）：Actuator 只暴露 `/actuator/health`（其余端点关闭、`show-details=never`）；应用补 `nosniff` / `X-Frame-Options: DENY` / `Referrer-Policy` |
| CSP / HSTS | **不在应用层，且在反代层落地**：HSTS 必须由 TLS 终止方下发；CSP 约束页面加载而后台静态站不由 Spring 托管。见 `docs/DEPLOYMENT-VERIFICATION.md` S 段 |
| 灾备演练 / 混沌工程 | **未实现** |

---

## 七、运营者最低自查清单

- [ ] 已读 `PRIVACY.md`，并按**自己的实际部署**改写「第三方 AI」一节
- [ ] 决定是否启用 L3 云端审核；若启用，已完成对群成员的告知（**含数据出境**）
- [ ] 已配置 `TGG_HASH_SALT`（缺失会退回开发兜底盐——userId 空间小，固定盐可被枚举反推）
- [ ] 已确认数据库访问受控（复核队列、信用分账本含**明文** id，用途是人工定位主体）
- [ ] 已决定保留策略（`tgg.retention.enabled`），并按期限向用户作出说明
- [ ] 已确认**未**依赖本项目提供制裁筛查 / KYC / 资金托管能力
- [ ] 已按所在法域完成必要的登记/备案（本项目**不代替**该义务）
